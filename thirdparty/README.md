# Third-party SDK

On Linux, `./build.sh --bootstrap-sdk` checks out the pinned ReXGlue 0.10
development commit recursively under `.deps/rexglue-sdk/`. An installed SDK
may instead be selected with `--sdk-prefix`.
